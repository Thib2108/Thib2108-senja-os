[project]
name = "senja-os"
version = "0.1.0"
description = "Senja OS monorepo — spec-driven (SDAI-CHAT-001..004)"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["packages/harness_kernel", "packages/contracts", "services/api"]

[tool.ruff]
line-length = 100

[tool.mypy]
strict = true
